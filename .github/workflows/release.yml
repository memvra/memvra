name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/goreleaser/goreleaser-cross:v1.23
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fix git safe directory
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Create darwin compiler wrappers
        run: |
          printf '#!/bin/sh\nexec o64-clang -Wno-unused-command-line-argument -Wno-error=unused-command-line-argument "$@"\n' > /usr/local/bin/darwin-amd64-cc
          printf '#!/bin/sh\nexec o64-clang++ -Wno-unused-command-line-argument -Wno-error=unused-command-line-argument "$@"\n' > /usr/local/bin/darwin-amd64-cxx
          printf '#!/bin/sh\nexec oa64-clang -Wno-unused-command-line-argument -Wno-error=unused-command-line-argument "$@"\n' > /usr/local/bin/darwin-arm64-cc
          printf '#!/bin/sh\nexec oa64-clang++ -Wno-unused-command-line-argument -Wno-error=unused-command-line-argument "$@"\n' > /usr/local/bin/darwin-arm64-cxx
          chmod +x /usr/local/bin/darwin-amd64-cc /usr/local/bin/darwin-amd64-cxx /usr/local/bin/darwin-arm64-cc /usr/local/bin/darwin-arm64-cxx

      - name: Run GoReleaser
        run: goreleaser release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
